function nonce = ClassicalTentGenerateNonce(N,x0)

x=x0;

mu = 1.99;

nonce = zeros(1,N,'uint32');

for i=1:N

    if x<0.5
        x = mu*x;
    else
        x = mu*(1-x);
    end

    nonce(i)=uint32(floor((2^32)*x));

end

end
